using UnityEngine;
using System.Collections;

public class backgroundDetails : MonoBehaviour
{

		public GameObject dust ;
		// int t;
		// int t2;

		// Use this for initialization
		void Start ()
		{
				// t2 = Random.Range (0, 30);
		}
	
		// Update is called once per frame
		void Update ()
		{
		// 		t++;

		// 		if (t > t2) {
		// 				Quaternion rR = Quaternion.Euler (globales.getRandomRot ());
		// 				Instantiate (dust, globales.getRandomPos (), rR);
		// 				t = 0;
		// 				t = Random.Range (0, 30);
		// 		}
		}
}
